// JavaScript code for photo carousel (you can use a library like Slick, Owl Carousel, or create your own)
// Example:
$(document).ready(function(){
    $('.carousel').slick({
        autoplay: true,
        autoplaySpeed: 2000,
        arrows: false,
    });
});
